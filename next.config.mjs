/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
  images: {
    domains: ["p2tu4ppv4zyfhwxg.public.blob.vercel-storage.com"],
  },
};

export default nextConfig;
